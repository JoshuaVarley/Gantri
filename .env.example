# Gantri — example environment file
# Copy to .env and fill in your values. The .env file is loaded automatically
# by both the CLI and Worker hosts (lowest priority — overridden by real
# environment variables and user secrets).
#
# Usage:
#   cp .env.example .env
#   # edit .env with your actual keys
#
# Alternatively, use dotnet user-secrets for development:
#   cd src/Hosts/Gantri.Cli
#   dotnet user-secrets set "AZURE_OPENAI_API_KEY" "sk-..."

# Azure OpenAI / Azure AI Foundry
AZURE_OPENAI_API_KEY=

# Brave Search (used by the brave-search MCP server)
BRAVE_API_KEY=
